export enum SkillOperationStatus {
  Planned = "planned",
  Ready = "ready",
  AlreadyExists = "already-exists",
  Replace = "replace",
  AlreadyInstalled = "already-installed",
}

export enum SkillUpdateAvailability {
  Unknown = "unknown",
  UpToDate = "up-to-date",
  UpdateAvailable = "update-available",
}
